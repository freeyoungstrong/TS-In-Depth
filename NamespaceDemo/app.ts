/// <reference path="utility-functions.ts" />

const result = Utility.maxBooksAllowed(30);
console.log(result);

import util = Utility.Fees;
const result2 = util.calculateLateFee(90);
console.log(result2);
